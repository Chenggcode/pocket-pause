# 参与贡献

感谢你帮助改进 Pocket Pause。提交改动前，请先确认它服务于可靠提醒、低打扰和本地优先的产品目标。

## 开发环境

- Node.js 22 或更高版本
- pnpm 11 或更高版本
- Windows 或 macOS

```powershell
pnpm install --frozen-lockfile
pnpm verify
pnpm start
```

## 提交问题

- 功能异常请提供操作系统、应用版本、复现步骤、预期行为和实际行为。
- 涉及多个显示器时，请补充缩放比例和屏幕排列。
- 安全问题不要提交公开 Issue，请按照 [SECURITY.md](SECURITY.md) 报告。

## 提交代码

1. 从 `main` 创建简短、单一目的的分支。
2. 保持改动聚焦，不混入无关格式化或重构。
3. 为调度、设置校验、存储等逻辑变化补充测试。
4. 执行 `pnpm verify`，并手动验证受影响的 Electron 界面。
5. 在 Pull Request 中说明行为变化、验证方式和仍存在的风险。

## 设计约束

- 主进程是提醒和设置状态的唯一可信来源。
- 渲染进程只能使用 preload 暴露的业务接口。
- 不引入账户、遥测或网络请求，除非经过明确的产品与隐私评审。
- 新增外部资源前必须确认许可证允许随应用重新分发。
